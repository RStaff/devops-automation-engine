name: CI – ad analytics Pipeline

on:
  push:
    paths:
      - 'ad_analytics_pipeline/**'
  workflow_dispatch:

jobs:
  run-ad_analytics-pipeline:
    uses: ./.github/workflows/ci-template.yml
    with:
      pipeline-dir: ad_analytics_pipeline
      environment: dev
      tools: '["stub1","stub2","stub3"]'
