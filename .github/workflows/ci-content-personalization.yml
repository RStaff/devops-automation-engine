name: CI – Content Personalization Pipeline

on:
  push:
    paths:
      - 'content_personalization_pipeline/**'
  workflow_dispatch:

jobs:
  run-content-personalization-pipeline:
    uses: ./.github/workflows/ci-template.yml
    with:
      pipeline-dir: content_personalization_pipeline
      environment: dev
      tools: '["segment","render","validate"]'
