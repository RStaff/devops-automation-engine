name: CI – MLOps Pipeline

on:
  push:
    paths:
      - 'mlops_pipeline/**'
  workflow_dispatch:

jobs:
  run-mlops-pipeline:
    uses: ./.github/workflows/ci-template.yml
    with:
      pipeline-dir: ingestion_pipeline/financial
      environment: dev
      tools: '["train","validate","deploy"]'
